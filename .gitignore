node_modules/
dist/
.cache/
coverage/
*.log

# Credentials must never be committed
.env
.env.*
!.env.example

# tribes auth material (private keys, JWT cache, wallet records) — never commit
.tribes/

# Mutable runtime state (venue status snapshots etc.)
runtime/
