# @tribes-terminal/skill-transaction

Shared transaction/auth skill for ATA.

This is a CLI-only package. It intentionally has no `src/index.ts` entrypoint and no
`exports` mapping in `package.json`.
