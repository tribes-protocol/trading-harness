---
description: Scout the best actionable Hyperliquid trade idea and stress-test it through the bull/bear desk. Research-only by default.
argument-hint: "[optional focus hint like 'AI coins', 'stocks only', or 'commodities only']"
---

Follow the `thesis` skill (`.pi/skills/thesis/SKILL.md`) in scout mode.

$@

Let technical structure and volatility set the entry, target, and invalidation for the requested
horizon; do not impose a fixed percentage bracket. Cover crypto, securities, and commodities
unless I scoped the request. The judge makes a categorical recommendation; a coherent conditional
setup needs an explicit, checkable trigger.
Write the outcome to a proposal file; do not touch `.tribes/thesis/current.md` if it holds an
active thesis, and do not execute anything unless I explicitly authorized a judge-approved,
safety-cleared auto-entry.
